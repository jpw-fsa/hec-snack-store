# Snack Store — HEC bake-off sandbox

Three steps:

```bash
npm install && npx playwright install chromium
npm run dev          # http://localhost:5173
claude               # then: "register me for the workshop"
```

Your mission is in [TICKET.md](./TICKET.md). When you're done, tell Claude: `/submit`.
